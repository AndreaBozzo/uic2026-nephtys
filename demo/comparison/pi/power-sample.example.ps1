param()

# Replace this body with the selected meter's local API call. Keep the output
# schema stable: cumulative energy must be monotonic for the entire benchmark.
throw 'Configure the wall power meter adapter before running the Pi benchmark. Output JSON: {"watts":5.2,"energy_wh":123.456}'
