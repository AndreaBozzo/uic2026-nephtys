# Retained publication comparison

All six planned slots completed on their first attempt. `runs.csv` contains the
valid raw rows and `summary.md` contains arithmetic mean and sample standard
deviation by system. The per-slot JSON files are the machine-readable source
records; process and collector logs are retained alongside them.

No invalid-attempt file exists for this run. The accepted-event sequence hash
matched between Nephtys and Node-RED for every paired trial.
